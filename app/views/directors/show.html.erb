<h1><%= @director.name %></h1>

<h3>Currently Active: <%= @director.active %> </h3>
<h3>Awards Won: <%= @director.awards_won %></h3>
<h3>Movie Count: <%= @director.total_movies %></h3>

<%= link_to "#{@director.name} Movies", "/directors/#{@director.id}/movies" %>
<%= link_to "Update Director", "/directors/#{@director.id}/edit" %>

<%= link_to "Delete Director", "/directors/#{@director.id}", method: :delete %></p>
